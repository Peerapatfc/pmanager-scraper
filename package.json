{
	"name": "playwright-login-test",
	"version": "1.0.0",
	"description": "Playwright tests for login functionality",
	"scripts": {
		"start": "node scrape-all-with-details.js",
		"test": "playwright test",
		"test:headed": "playwright test --headed",
		"test:ui": "playwright test --ui",
		"install:browsers": "playwright install"
	},
	"dependencies": {
		"@playwright/test": "^1.40.0",
		"dotenv": "^17.2.3",
		"googleapis": "^161.0.0",
		"playwright": "^1.40.0"
	}
}
